#nocap;

struct Cell {
  content: Int
}

package Test {

    main fn testF(arr String) {
      val a = arr arr Cell[1];
      a[0] = [new Cell(42)];
      console.print(a[0][0].content as String);
    }

}
